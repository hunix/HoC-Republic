import { _WebSocketServer } from "ws";

// The bundle exposes a lot, but we might have to grab it from the default or specific exports.
// If it's not exported cleanly, we can just use tsx to run our shim natively!
console.log("Mock Gateway wrapper initialized...");
